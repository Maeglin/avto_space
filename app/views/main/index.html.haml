.slider-wrapper.theme-default
  .ribbon
  .nivoSlider{:id => "slider"}
    -Dir.foreach(Rails.root.join('app', 'assets', 'gallery').to_s) do |x|
      -if x!='.' && x!='..'
        %a{:href => [root_path]}
          %img{:src => [asset_path x], :alt => '', :title => '#htmlcaption'}
  .nivo-html-caption{:id => "htmlcaption"}
    %p
      ПОЛИМЕРНАЯ КЕРАМИКА
    %p
      Совершенная защита кузова автомобиля
    %a{:href => "#"}
      подробнее >>

:javascript
  $(window).load(function() {
        $('#slider').nivoSlider();
  });
